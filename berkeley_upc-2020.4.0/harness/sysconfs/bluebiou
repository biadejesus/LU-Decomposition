network = ibv
batch_sys = slurm
queues = [
        {
          Q_name     => commons,
          Q_maxnode  => 8,
          Q_minnode  => 1,
          Q_maxtpn   => 16,
          Q_maxtime  => 04:00:00,
        }
]

#repository = N/A

nthread_default = 4
max_proc_per_node = 8
min_num_nodes = 1

submit_cmd = sbatch

run_env_default = { MPIRUN_CMD => 'srun -K0 -W30 %V -mcyclic -n %N %C',
		    GASNET_PHYSMEM_MAX => '200G',
		    GASNET_PHYSMEM_NOPROBE => 1 }
